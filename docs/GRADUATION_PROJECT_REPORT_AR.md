# 🎓 تقرير مشروع نظام الحضور الذكي للطلاب (Student Attendance System)

## 📌 نبذة عن المشروع
نظام الحضور الذكي هو منصة ويب متكاملة تهدف إلى أتمتة وتحسين عملية تسجيل حضور الطلاب في الجامعات والمؤسسات التعليمية. يستخدم النظام تقنيات حديثة مثل رموز الاستجابة السريعة (QR Codes) الديناميكية، والموقع الجغرافي (Geofencing)، وبصمة الجهاز (Device Fingerprinting) لضمان دقة وموثوقية بيانات الحضور ومنع التلاعب.

---

## 🏗️ هيكلية المشروع (Project Architecture)

المخطط التالي يوضح البنية التحتية للنظام وكيفية تدفق البيانات بين المكونات المختلفة:

```mermaid
graph TD
    %% Users
    UserStudent[👤 الطالب]
    UserTeacher[👨‍🏫 الأستاذ]
    UserAdmin[👔 الإداري]

    %% Frontend
    subgraph Frontend [واجهة المستخدم (React + Vite)]
        Auth[🔐 نظام المصادقة (Auth Context)]
        Pages[📄 الصفحات ولوحات التحكم]
        QR_Scan[📷 ماسح الـ QR]
        API_Client[🌐 عميل الـ API (Axios)]
    end

    %% Backend
    subgraph Backend [الخادم (Node.js + Express)]
        Gateway[🛡️ بوابة الأمان (Helmet/CORS)]
        Router[🔀 الموجه (Routes)]
        
        subgraph Controllers [وحدات التحكم]
            AuthCtrl[تحكم المصادقة]
            AttendCtrl[تحكم الحضور]
            PromoCtrl[تحكم الترحيل]
        end
        
        subgraph Services [خدمات المعالجة]
            QRService[خدمة الـ QR الديناميكي]
            GeoService[خدمة الموقع الجغرافي]
            PromoService[خدمة الترحيل والتحميل]
        end
        
        ORM[💎 Prisma ORM]
    end

    %% Database
    subgraph Database [قاعدة البيانات]
        Postgres[(�️ PostgreSQL)]
    end

    %% Interactions
    UserStudent -->|يمسح الكود| QR_Scan
    UserTeacher -->|ينشئ جلسة| Pages
    UserAdmin -->|يدير النظام| Pages

    Pages --> Auth
    Auth --> API_Client
    API_Client -->|HTTPS Request| Gateway
    Gateway --> Router
    Router --> AuthCtrl & AttendCtrl & PromoCtrl
    
    AuthCtrl --> ORM
    AttendCtrl --> QRService & GeoService
    PromoCtrl --> PromoService
    
    QRService --> ORM
    PromoService --> ORM
    ORM --> Postgres
    
    %% Styles
    style Frontend fill:#e1f5fe,stroke:#01579b
    style Backend fill:#e8f5e9,stroke:#2e7d32
    style Database fill:#fff3e0,stroke:#e65100
```

---

## �🛠️ التقنيات المستخدمة (Technology Stack)

### الواجهة الأمامية (Frontend)
- **Framework**: React.js مع Vite للأداء العالي.
- **Language**: TypeScript لضمان سلامة الكود.
- **State Management**: TanStack Query (React Query) لإدارة البيانات والتخزين المؤقت.
- **UI Components**: Shadcn UI + Tailwind CSS لتصميم عصري ومتجاوب.
- **PWA Capabilities**: دعم العمل كتطبيق ويب تقدمي (قابل للتثبيت).

### الواجهة الخلفية (Backend)
- **Runtime**: Node.js.
- **Framework**: Express.js لبناء الـ API.
- **Database**: PostgreSQL (قاعدة بيانات علائقية قوية).
- **ORM**: Prisma للتعامل الآمن والسهل مع قاعدة البيانات.

---

## 📈 نظام الترحيل والتحميل (Academic Promotion System)

يحتوي النظام على محرك ذكي لمعالجة نتائج الطلاب وترحيلهم بين المراحل الدراسية آلياً وفق القوانين الجامعية المعتمدة.

### 1. قواعد النجاح والرسوب (Success & Failure Rules)
يقوم النظام بحساب حالة الطالب النهائية بناءً على عدد المواد التي رسب فيها:

*   **ناجح بالكامل (Promoted)**: إذا نجح الطالب في جميع المواد. يتم ترحيله للمرحلة التالية بصفة "منتظم".
*   **ناجح بالتحميل (Promoted with Carry)**: إذا رسب الطالب في عدد مواد **أقل من أو يساوي الحد المسموح** (افتراضياً مادتين). يتم ترحيله للمرحلة التالية ولكنه يُطالب بإعادة امتحان المواد التي رسب فيها (مواد التحميل).
*   **راسب (Repeat Year)**: إذا تجاوز عدد المواد الراسب فيها الحد المسموح (أكثر من مادتين). في هذه الحالة، يبقى الطالب في نفس المرحلة ويعيد السنة (يعتبر "معيد").

### 2. إعدادات الترحيل (Promotion Configuration)
يمكن لرئيس القسم تخصيص قوانين الترحيل لتناسب الكلية:
*   **حد التحميل الأقصى**: عدد المواد المسموح بتحميلها (افتراضياً 2).
*   **مواد العبور (Core Subjects)**: إمكانية تحديد مواد "أساسية" يمنع تحميلها (يجب النجاح فيها حصراً للعبور).
*   **السنة الأخيرة**: خيار لمنع التحميل لطلاب السنة النهائية (يجب تصفية جميع المواد للتخرج).

### 3. آلية تنفيذ الترحيل
1.  **المعاينة (Preview)**: يقوم الإداري بطلب معاينة النتائج قبل اعتمادها. يعرض النظام قائمة بكل الطلاب وحالتهم المتوقعة (ناجح، تحميل، راسب).
2.  **التنفيذ (Execute)**: عند الاعتماد، يقوم النظام بـ:
    *   نقل الطلاب الناجحين للمرحلة التالية.
    *   تسجيل مواد المرحلة الجديدة لهم تلقائياً.
    *   تسجيل "مواد التحميل" للطلاب المحملين في سجلاتهم للسنة الجديدة.
    *   إبقاء الطلاب الراسبين في مرحلتهم الحالية وإعادة تسجيلهم في المواد الراسبة فقط.

---

## 🌟 المميزات الرئيسية (Key Features)

### 1. نظام الحضور الذكي (Smart Attendance)
- **Dynamic QR Codes**: إنشاء رموز QR تتغير تلقائياً كل 30 ثانية.
- **Geofencing**: التحقق من موقع الطالب الجغرافي (GPS).
- **Device Fingerprinting**: ربط الحساب بجهاز الطالب.

### 2. لوحات تحكم مخصصة (Dashboards)
*   **لوحة الإدارة**: إدارة كاملة للبيانات والترحيل والتقارير.
*   **لوحة الأستاذ**: إدارة الجلسات ومراقبة الحضور الحي.
*   **لوحة الطالب**: تسجيل الحضور ومتابعة السجلات.

---

## ⚙️ آلية عمل تسجيل الحضور (Workflow)

1.  **إنشاء الجلسة**: يختار الأستاذ المادة والقاعة.
2.  **توليد الرمز**: يعرض النظام رمزاً مشفراً يتغير زمنياً.
3.  **المسح**: يمسح الطالب الرمز.
4.  **التحقق الثلاثي**: خادم النظام يتحقق من (صحة الرمز + الموقع الجغرافي + بصمة الجهاز).
5.  **التسجيل**: يتم تسجيل الحضور فوراً في قاعدة البيانات.

---

## 🔐 تدابير الأمان (Security Measures)

1.  **حماية التزييف**: تغيير الرمز يمنع الصور الثابتة.
2.  **التحقق المكاني**: يمنع التسجيل من خارج القاعة.
3.  **تشفير البيانات**: جميع الاتصالات HTTPS وكلمات المرور Bcrypt.
4.  **Rate Limiting**: حماية من هجمات الضغط على الخادم.

---

هذا النظام يمثل حلاً تقنياً متكاملاً وشاملاً لإدارة الحياة الأكاديمية من الحضور وحتى الترحيل والتخرج.
